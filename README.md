# Color-Detection-Using-Python
A Python-based Color Detection Application with login authentication using SQLite. It detects colors from uploaded images and live camera feed using OpenCV, TensorFlow CNN, and a color dataset to display RGB, HEX values, and color names with an interactive and user-friendly GUI.
